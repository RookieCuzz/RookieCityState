# 常见问题

## 启动时提示 Vault 没有经济服务

除了 Vault，还必须安装一个能向 Vault 注册经济服务的经济插件。

## 占位符不显示

确认 PlaceholderAPI 已启用，并使用 `%rookiecitystate_...%`。EssentialsChat 使用 `<rookiecitystate_...>`。

## 重载失败

查看控制台中的具体配置路径。失败的重载不会覆盖当前有效配置。
